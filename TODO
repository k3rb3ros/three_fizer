* Break file reads/writes into block operations to speed up Disk/IO 
* add scheme to read buffer and encrypt the file in chunks instead of the entire file 
* Add que for chunks to encrypt
* Add que for chunks to write
* Write to a seperate file then rename to original file re-name on success
* Add data structure to pass data chunks around
* Add asynchronous fileIO
* parallelize decryption
* Add tests for Skein hashing *DONE*
* Add tests for cbc operations
* Add tests for fileIO
* Add tests for util 
* Add tests for pad operations
* Get code reviewed by a security expert
